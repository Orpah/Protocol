# ORPAH 协议规范仓（`Protocol`）开发规则

本仓 = **ORPAH 协议的唯一定义处**（"唯一事实来源"）：

| 文件 | 管什么 |
|---|---|
| `docs/OrpahIDProtocol.md` | 身份层：SN 编码（§2）/ 号码校验（§3）/ Unicode（§4）/ 数字签名与报文（§5）/ 密钥管理（§6）/ 承载链路适配（§7）/ 降级策略（§8）/ 后端接口（§9）/ 威胁模型（§10）/ Phase 2（§11）/ 附录与修订记录（§12） |
| `docs/orpah-over-halow/SPEC.md` | 链路规格：网络假设（§3）/ 报文集（§5）/ 传输封装（§6）/ 走失表（§7）/ 安全与隐私（§8）/ 实现进度（§9）/ **开放问题清单（§10）** |
| `docs/OrpahIDSpace.md` | SN 空间规划 |
| `docs/OrpahProtocol.odg`、`docs/OrpahProtocol_zh.odg` | 原图（网络结构/消息流等）；导出件在 `docs/images/` |

**本仓只有文档，没有代码**（没有实现、没有测试、没有 CI）。

> 详细内容见 `README.md`（英）/ `README_zh.md`（中）与 `docs/`；本文件只留「必须照着做 / 不能做」的部分。

## 0. 与另外四个仓的关系（最要紧的一条）

| 仓 | 它实现什么 | 与协议的关系 |
|---|---|---|
| **本仓** | 规范本身 | **权威** —— 报文字段/签名流程/算法一律以本仓为准 |
| `halow-demo`（`simulator/`） | 空口 / 设备 / 长距（不碰业务） | 按 §7 承载链路适配实现 |
| `orpah-over-halow` | 业务全链路（server / Router 桥 / 定位 / 告警 / UI）+ **Python 参考实现**（`orpah_id.py`、`damm32.py`…） | 严格按本仓；**黄金样本与跨语言交叉测试在这里**（`c/run_cross_test.py`、`c/test_vectors*.txt`） |
| `orpah-openwrt-demo` | 路由器端 OpenWrt 软件 | 同上；它的 `vendor/orpah/` 是**逐字节复制**的实现，不许改 |
| `orpah-client-demo` | 客户端硬件 + 固件 | 同上；`proto/` 是 C 实现的协议内核 |

- **别的仓不许私改协议**：出现「必须改协议才能实现」时，**回本仓改** → 再让各实现跟上；
  实现仓若有 `vendor/`，改完要更新它的 `VENDOR.md` 里的来源提交与 hash。
- 反向也要走通：实现里量出来的**真机事实/阈值**要**回填进规范**（SPEC 的"实测回填"就是这么来的），
  否则规范会慢慢变成"纸面规范"。

## 1. 改规范 = 改契约（每次都必须做的三件事）

1. **改正文**；
2. **在 §12 附录的修订记录表加一行**（版本号 + 日期 + 变更），并把**标题行的版本号**同步
   —— 现在 = `# Orpah ID 协议规范 v1.20`，标题与修订记录表必须一致（这本身就是 §1.6 那条修订）
3. **说清会打破什么**：签名预像 / JSON 规范化（JCS）/ `b64url` / 黄金样本 / CHECK 算法这几处一动，
   已入库密钥与测试向量就会全部对不上 → 必须在修订行里**点名要求各实现重跑交叉测试**。
   （例：§3 的 `CHECK` **只由 `ORG-UNIQUE` 算、不含 CC** —— 改这条等于让所有已发 SN 的校验位失效。）

- 规范里**不许留"应该/可能"的含糊句**：要么写成 MUST，要么进 §11 Phase 2 待验证项 / SPEC §10 开放问题。
- **数字要有出处**：能回答"怎么量出来的"（真机实测 / 零假设分布）；拍脑袋的阈值不许进规范。
- 规范改了 → 实现仓的交叉测试 / `run_checks.py` 必须仍全绿，这是改规范的最小验收。

## 2. 措辞硬规则（规范会被人引用，写错就是系统性误导）

- **偏差 ≠ 作恶**：不得用「谎报 / 撒谎 / 欺骗 / 作弊 / 有罪」这类归因词，统一写
  「偏差 / 不一致 / 离群 / 对不上」，并带「**仅线索、需人工复核**」；成因列举必须含
  遮挡 · 多径、天线或接头、元件老化、标定漂移、（其中一种）被改装/冒充。
- **精度 ≠ 可信度**，两者分开描述。**孤证不立**是本项目原则（`§5.7` + SPEC §8 原则 **P-1**）：
  单条**机器**证据不足以支撑位置判定；**唯一例外** = 证据明确来自人（办案人 / 搜救队），单源可采信
  但必须留痕。**k 台 Router 合谋不可防**（信息论边界，n−k ≥ 3 才谈定位）—— 不得写成"已防住"。
- **不得把"没做 / 未实测"写成"已支持"**：未验证的一律标「未验证」（例：真机标定 F-11、
  SPEC §6 的 DHCP/静态 IP 仍待定、Phase 1 只做 IPv4）。
- **密钥生命周期**：设备侧 1 把 ECDSA P-256（Slot 0）+ 1 把 32 字节 HMAC（降级用），
  **一代终身、不轮换**（§6.3.1）；撤销是唯一的生命周期操作（§6.3.2）。demo 里的
  `rotate/grace/retired` **只是"对比用的演示代码"，不是设备模型**，不得当成系统能力
  （SPEC §10 **F-13 已结案**）。
- **已否决的做法不再提**（§5.7 的 1.18 修订 + SPEC）：Router 身份白名单 / 受信集合（只回答"谁在说"）、
  靠运营商绑定 IP 当设备身份、密钥轮换。
- **已知边界要写在规范里而不是藏起来**：`xport`（路由器侧观测）**不在签名预像内** →
  能冒充路由器就能伪造定位（SPEC §10 **F-12**）；下行真实性靠 Server 签名 + 新鲜性
  （`dts`/`dn`，SPEC §10 **F-14**），而**空口那段（Router → Client）仍无认证**。

## 3. 规范要可验收（跟实现对齐）

- 规范里的样例/常量必须能被实现**逐字节复现**：黄金样本（如 `WH01-9AF3C1D2 → B`）、
  测试向量、Damm32 拟群构造（`T[x][y] = 2·(x⊕y)`，`p(t)=t⁵+t+1`，附录 B）——
  Python 参考实现 ↔ C 实现零偏差（`run_cross_test.py`）。
- "由实现决定"的部分要**在规范里写明定性约束与下限/上限**（例：设计常态 **60 s** 连接周期、
  降级**下限 L1、永不 L3**、限频参数前缀、无 RTC 时 `ts=0`）。
- 规范里引用的实现细节要**指向单一源**（例：常态周期 = `orpah-over-halow/energy.NORMAL_INTERVAL_S`），
  不要在两处各写一个数。

## 4. 文档/README：图文并茂（2026-09-21 用户定，适用于所有项目）

- 每个 README（顶层 + 子目录）**正文里**要嵌图 + 一句 caption（`[![说明](docs/images/x.png)](…)`），
  用户原话「**图对人的阅读吸引力更大**」；别只给文字表格、也别只放一个链接。
- **中英两份要同步**：`README.md`（英）与 `README_zh.md`（中）成对，图也有 `_zh` 版；
  只改一边 = 另一边过时（本仓 `docs/images/` 里 `*_zh.png` 就是为此存在）。
- **图要与规范正文一致**：`.odg` 是原图源、`docs/images/*.png` 是导出件 ——
  改图要**连正文一起改**，别只改图（图里多一个箭头、正文没写，就会互相打架）。
- Markdown 在本仓**正文里可以正常用**（这是给人读的规范）；「页面/i18n 文案里不许写 Markdown」
  那条属于 UI 仓（见 `orpah-over-halow/AGENTS.md`）。

## 5. 许可与提交纪律

- `LICENSE` = **Apache-2.0**；本仓另有一份 **`NOTICE`**（版权行：Langhua Ltd.）——
  这是**唯一**带 NOTICE 的仓，兄弟仓按做法只放 `LICENSE`（别把 NOTICE 复制过去）。
- 引用第三方素材（图片、算法、表格）要注出处；**只提交我自己创建的文件**：别人的手册/PDF/SDK/
  截图一律不提交（入库即无授权传播）。
- 提交前 `git rev-parse --show-toplevel` 确认仓库；**禁止 `git add -A`**，一律显式写路径；
  提交后 `git show --stat` 核一遍清单。**push 永远由用户自己做。**
- 文本一律 **UTF-8 无 BOM**；`.vscode/`、`__pycache__/`、`*.pyc` 已在 `.gitignore` 里，别撤。

## 6. 现状与未做（如实）

- 本仓**只有文档**：`docs/`（5 份：两份规范 + `OrpahIDSpace.md` + 两份 `.odg` 原图）、`docs/images/` **7 张图**、
  `README.md` / `README_zh.md`、`LICENSE` / `NOTICE`。**没有代码、没有测试、没有 CI。**
- `OrpahIDProtocol.md` = **v1.20**（修订记录逐版可查）；`SPEC.md` 标题仍写「草稿 → L1/L2/L3 实测回填」：
  L1/L2/L3/L3b/L3c 已定稿，Phase 1 **只做 IPv4**。
- 未结项：SPEC §10 开放问题清单（F-11 真机标定数值、§6 的 DHCP/静态 IP…）、
  `OrpahIDProtocol.md` §11 Phase 2 待验证项。
